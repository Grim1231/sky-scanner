[project]
name = "sky-scanner-core"
version = "0.1.0"
description = "Shared models, schemas, and utilities"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.10.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sky_scanner_core"]
